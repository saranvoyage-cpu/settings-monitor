name: Test Sensitive File Change

on:
  push:
    paths:
      - 'setting2.txt'

jobs:
  test-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm trigger
        run: echo "✅ Workflow triggered for setting2.txt change!"
